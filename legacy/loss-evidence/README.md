# Loss Evidence (legacy)

Temporarily removed from the main site.

## Contents
- `section.html` — original Loss Evidence section markup
- `loss.css` — related styles
- `loss-charts.js` — chart loading/rendering logic (excerpt from `js/app.js`)
- `data/` — baseline/buggy loss CSV files

## Restore
1. Paste `section.html` back into `index.html` (after hero).
2. Re-add nav link: `<a href="#loss">Loss Evidence</a>`
3. Merge `loss.css` into `css/styles.css` and `loss-charts.js` into `js/app.js`.
4. Ensure `assets/data/*.csv` exist (copies are in `data/` here).
